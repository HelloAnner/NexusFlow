# 文档总览

本目录用于承接从原始需求到可实施产品设计的拆分文档。顶层 `docs/prd.md` 是总 PRD，本目录下的领域文档用于进一步描述功能、页面、技术实现、字段、状态、权限和跨模块联动。

## 目录结构

| 目录 | 领域 | 说明 |
| --- | --- | --- |
| `00-overview` | 总览 | 产品范围、全局对象关系、架构设计、子模型实现 |
| `01-organization-personnel` | 组织人员 | 组织树、人员档案、技能标签、人员详情 |
| `02-permission` | 权限 | 角色权限、数据范围、隐藏项目和跨部门边界 |
| `03-task-management` | 任务 | 任务模型、状态流、任务列表、任务详情、分工与验收 |
| `04-dispatch-collaboration` | 派发协同 | 任务派发、跨部门协调、后补填报 |
| `05-load-conflict` | 负载冲突 | 工时占用、负载日历、冲突中心、风险规则 |
| `06-resource-library` | 资料库 | 资料模型、版本、归档、资料页面 |
| `07-home-dashboard` | 首页看板 | 不同角色首页、甘特图、快捷入口 |
| `08-config-center` | 配置中心 | 字典、模板、流程、告警、页面配置 |
| `09-notification-report` | 通知报表 | 待办、通知、指标口径、报表页面 |
| `10-tool-center` | 工具台 | 常用工具、智能体工具、入口配置 |
| `11-project-management` | 项目管理 | 项目模型、项目成员、隐藏项目、项目归档 |
| `12-gantt-search` | 甘特搜索 | 甘特图、全局搜索、权限裁剪 |
| `13-admin-auth` | 账号后台 | SA 后台、邀请注册、账号安全、角色入口 |

## 阅读顺序

1. 先读 `product-scope.md`，确认系统目标、一期范围和边界。
2. 再读 `domain-model.md`，理解组织、人员、项目、任务、分工、资料、审批之间的关系。
3. 技术评审读 `architecture.md` 和 `submodel-implementation.md`，确认 Rust 后端、TS+Bun 前端、单二进制部署、`.env` 基础组件配置和子模型实现边界。
4. 按领域阅读功能设计、页面设计和技术设计。
5. 开发前重点核对 `02-permission`、`03-task-management`、`04-dispatch-collaboration`、`05-load-conflict`、`08-config-center`、`11-project-management`、`12-gantt-search`、`13-admin-auth`，这些模块决定系统能否形成完整闭环。

## 设计原则

- 统一对象：项目作为任务归属和统计口径，临时工作、出差、学习、休假、安全等工作事项统一抽象为任务。
- 统一入口：所有角色从首页进入待办、任务、甘特图和风险处理。
- 统一注册：邀请注册按角色、组织和项目预绑定，注册后进入对应角色首页。
- 统一权限：所有页面都使用角色、组织、项目、任务可见性四类规则裁剪数据。
- 统一配置：任务类型、字段、流程、资料要求、告警规则尽量配置化。
- 统一留痕：任务创建、派发、审批、变更、汇报、验收、归档必须可追溯。
- 统一部署：系统最终以单二进制启动，默认端口 `8089`，基础组件地址通过 `.env` 配置。
